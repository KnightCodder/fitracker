import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.KnightCodder.fitracker',
  appName: 'fitracker',
  server: {
    url: 'https://fitracker-five.vercel.app/'
  }
};

export default config;
